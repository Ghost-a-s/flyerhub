import { templates } from "@/lib/data";
console.log(
  `Seed contract ready for ${templates.length} mock catalog listings.`,
);
console.log(
  "Use db/schema.ts and npm run db:generate before inserting records in a connected PostgreSQL environment.",
);
