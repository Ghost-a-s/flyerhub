"use client";
import { useQuery } from "@tanstack/react-query";
import { api } from "@/lib/api/client";
import { AuthForm } from "@/components/auth-form";
export default function AdminPage() { const query = useQuery({ queryKey: ["admin", "analytics"], queryFn: async () => (await api.get("/admin/analytics")).data.data }); if (query.isError) return <main className="container-page py-20"><div className="mx-auto max-w-md rounded-xl border p-7"><p className="eyebrow">Administration</p><h1 className="mt-3 font-display text-3xl font-bold">Admin sign in</h1><div className="mt-6"><AuthForm mode="admin" /></div></div></main>; if (query.isLoading) return <main className="container-page py-20 text-muted-foreground">Loading administration…</main>; return <main className="container-page py-12"><p className="eyebrow">Administration</p><h1 className="mt-3 font-display text-4xl font-bold">Keep the library healthy.</h1><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{Object.entries(query.data).map(([label, value]) => <div className="rounded-xl border bg-card p-5" key={label}><p className="text-3xl font-bold">{String(value)}</p><p className="mt-1 text-sm capitalize text-muted-foreground">{label}</p></div>)}</div></main>; }
