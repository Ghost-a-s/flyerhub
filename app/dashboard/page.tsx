"use client";
import Link from "next/link";
import { authClient } from "@/lib/auth-client";
export default function DashboardPage() { const { data, isPending } = authClient.useSession(); if (isPending) return <main className="container-page py-20 text-muted-foreground">Loading your workspace…</main>; if (!data?.user) return <main className="container-page py-20"><h1 className="font-display text-4xl font-bold">Your A.T workspace</h1><Link className="button-primary mt-6" href="/login">Sign in</Link></main>; return <main className="container-page py-12"><p className="eyebrow">Workspace</p><h1 className="mt-3 font-display text-4xl font-bold">Welcome back, {data.user.name}.</h1><p className="mt-3 text-muted-foreground">Manage submissions, saved references, and PSD downloads from one durable account.</p><div className="mt-8 flex gap-3"><Link className="button-primary" href="/contributor">Submit content</Link><Link className="button-quiet" href="/templates">Browse library</Link></div></main>; }
