import Link from "next/link";
const categories = ["church-flyers", "birthday-flyers", "business-flyers", "party-flyers", "restaurant-flyers", "event-flyers"];
export default function CategoriesPage() { return <main className="container-page py-12"><p className="eyebrow">Collections</p><h1 className="mt-3 font-display text-4xl font-bold">Find your next direction.</h1><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{categories.map((slug) => <Link key={slug} href={`/categories/${slug}`} className="rounded-xl border bg-card p-6 font-semibold capitalize hover:border-primary">{slug.replaceAll("-", " ")}</Link>)}</div></main>; }
